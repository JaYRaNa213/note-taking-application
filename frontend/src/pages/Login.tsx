import React from 'react';
import { Container, Typography } from '@mui/material';
export default function Login() {
  return (
    <Container sx={{ pt: 6 }}>
      <Typography variant="h5">Login page (use signup to request OTP)</Typography>
    </Container>
  );
}
